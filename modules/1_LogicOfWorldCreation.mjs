  // 1. UNKNOWN LOGIC OF WORLD CREATION.
  export const createWorld = async () => {
    //...
    const initialize = async _GodSHiddenThoughts => {
      const generatedPresets = null
      return generatedPresets
    }

    const bigBang = async _generatedPresets => {
      const entities = null
      return entities
    }

    const toDevelopCivilization = async _entities => {
      const output = {
        populationIsBroken: Boolean(Math.round(Math.random())),
        destroyTheWorld: null,
        rethinking: async _thoughts => _thoughts,
      }
      return output
    }
    return {
      initialize,
      bigBang,
      toDevelopCivilization,
    }
  };